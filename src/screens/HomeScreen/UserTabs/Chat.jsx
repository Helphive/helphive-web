import React from "react";

// You can use Material-UI or any other web UI library
import { Typography } from "@mui/material"; // Example using Material-UI

const Chat = () => {
    return <Typography variant="h5">Chat</Typography>; // Use Typography for text display
};

export default Chat;
